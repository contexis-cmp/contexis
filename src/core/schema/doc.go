// Package schema provides validation utilities for Contexis context files.
//
// It includes lightweight YAML checks and optional JSON Schema validation
// for `.ctx` files used to define agent contexts.
package schema
